# Error covariance matrices
covariance:

  # Error covariance matrix in model space
  B:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_climatological_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_climatological_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_climatological_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_climatological_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B.output.matrix_pseudo_inverse.path}

  # Error covariance matrix in model space
  B_multi_stdn_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_multi_stdn_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_multi_stdn_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_stdn_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_stdn_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_stdn_temporal.output.matrix_pseudo_inverse.path}


  # Error covariance matrix in model space
  B_uni_stdn_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_uni_stdn_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_uni_stdn_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_stdn_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_stdn_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_stdn_temporal.output.matrix_pseudo_inverse.path}

  # Error covariance matrix in model space
  B_multi_norm_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_multi_norm_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_multi_norm_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_norm_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_norm_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_norm_temporal.output.matrix_pseudo_inverse.path}


  # Error covariance matrix in model space
  B_uni_norm_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_uni_norm_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_uni_norm_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_norm_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_norm_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_norm_temporal.output.matrix_pseudo_inverse.path}

  # Error covariance matrix in model space
  B_multi_phys_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_multi_phys_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_multi_phys_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_phys_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_multi_phys_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_multi_phys_temporal.output.matrix_pseudo_inverse.path}


  # Error covariance matrix in model space
  B_uni_phys_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/B_uni_phys_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix.path}
      # Cholesky decomposition of the covariance matrix
      cholesky:
        # File path to save/load the Cholesky decomposition of the covariance matrix
        path: data/normalization/B_uni_phys_temporal_cholesky.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.cholesky.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.cholesky.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_phys_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix_inverse.path}
      # Inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/B_uni_phys_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.B_uni_phys_temporal.output.matrix_pseudo_inverse.path}


  # Error covariance matrix in observation space
  R:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/R_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.matrix.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/R_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.matrix_inverse.path}
      # Pseudo-inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the pseudo-inverse of the covariance matrix
        path: data/normalization/R_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.matrix_pseudo_inverse.path}
      # Correlation matrix
      correlation:
        # File path to save/load the correlation matrix
        path: data/normalization/R_correlation.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.correlation.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.correlation.path}
      # Inverse of the correlation matrix
      correlation_inverse:
        # File path to save/load the inverse of the correlation matrix
        path: data/normalization/R_correlation_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.correlation_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.correlation_inverse.path}
      # Pseudo-inverse of the correlation matrix
      correlation_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the pseudo-inverse of the correlation matrix
        path: data/normalization/R_correlation_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R.output.correlation_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R.output.correlation_pseudo_inverse.path}

  # Error covariance matrix in observation space
  R_multi_null_temporal:
    # What the method produces as output
    output:
      # Covariance matrix
      matrix:
        # File path to save/load the covariance matrix
        path: data/normalization/R_multi_null_temporal_matrix.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix.path}
      # Inverse of the covariance matrix
      matrix_inverse:
        # File path to save/load the inverse of the covariance matrix
        path: data/normalization/R_multi_null_temporal_matrix_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix_inverse.path}
      # Pseudo-inverse of the covariance matrix
      matrix_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the pseudo-inverse of the covariance matrix
        path: data/normalization/R_multi_null_temporal_matrix_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.matrix_pseudo_inverse.path}
      # Correlation matrix
      correlation:
        # File path to save/load the correlation matrix
        path: data/normalization/R_multi_null_temporal_correlation.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation.path}
      # Inverse of the correlation matrix
      correlation_inverse:
        # File path to save/load the inverse of the correlation matrix
        path: data/normalization/R_multi_null_temporal_correlation_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation_inverse.path}
      # Pseudo-inverse of the correlation matrix
      correlation_pseudo_inverse:
        # Parameters
        params:
          # Condition number threshold for the pseudo-inverse (e.g., to handle near-singular matrices)
          rcond: 1e-3
        # File path to save/load the pseudo-inverse of the correlation matrix
        path: data/normalization/R_multi_null_temporal_correlation_pseudo_inverse.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation_pseudo_inverse.path}
        # Load function
        load:
          _target_: numpy.load
          _args_:
            - ${preparation.covariance.R_multi_null_temporal.output.correlation_pseudo_inverse.path}