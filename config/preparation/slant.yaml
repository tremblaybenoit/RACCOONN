# Compute slant path coordinates
slant:
  # Training set
  train:
    # Function
    _target_: data.slant_path.compute_slant_path
    _recursive_: false  # Do not instantiate nested objects
    # What the method needs as input
    input:
      # Coordinates
      lat: ${data.stage.train.vars.lat}
      lon: ${data.stage.train.vars.lon}
      pressure: ${data.stage.train.vars.pressure}
      # Meta data
      meta: ${data.stage.train.vars.meta}
      # Profiles
      prof: ${data.stage.train.vars.prof}
    output:
      lat:
        # Output path
        path: ${paths.data_dir}/Train2/lat_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.train.output.lat.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.train.output.lat.path}
            - ${data.dtype}
        normalization: ${data.stage.train.vars.lat.normalization}
      lon:
        # Output path
        path: ${paths.data_dir}/Train2/lon_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.train.output.lon.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.train.output.lon.path}
            - ${data.dtype}
        normalization: ${data.stage.train.vars.lon.normalization}
      z:
        # Output path
        path: ${paths.data_dir}/Train2/z_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.train.output.z.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.train.output.z.path}
            - ${data.dtype}
        normalization: null
      x:
        # Output path
        path: ${paths.data_dir}/Train2/x_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.train.output.x.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.train.output.x.path}
            - ${data.dtype}
        normalization: null
      y:
        # Output path
        path: ${paths.data_dir}/Train2/y_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.train.output.y.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.train.output.y.path}
            - ${data.dtype}
        normalization: null
  # Validation set
  valid:
    # Function
    _target_: data.slant_path.compute_slant_path
    _recursive_: false  # Do not instantiate nested objects
    # What the method needs as input
    input:
      # Coordinates
      lat: ${data.stage.valid.vars.lat}
      lon: ${data.stage.valid.vars.lon}
      pressure: ${data.stage.valid.vars.pressure}
      # Meta data
      meta: ${data.stage.valid.vars.meta}
      # Profiles
      prof: ${data.stage.valid.vars.prof}
    output:
      lat:
        # Output path
        path: ${paths.data_dir}/Val2/lat_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.valid.output.lat.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.valid.output.lat.path}
            - ${data.dtype}
        normalization: ${data.stage.valid.vars.lat.normalization}
      lon:
        # Output path
        path: ${paths.data_dir}/Val2/lon_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.valid.output.lon.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.valid.output.lon.path}
            - ${data.dtype}
        normalization: ${data.stage.valid.vars.lon.normalization}
      z:
        # Output path
        path: ${paths.data_dir}/Val2/z_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.valid.output.z.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.valid.output.z.path}
            - ${data.dtype}
        normalization: null
      x:
        # Output path
        path: ${paths.data_dir}/Val2/x_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.valid.output.x.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.valid.output.x.path}
            - ${data.dtype}
        normalization: null
      y:
        # Output path
        path: ${paths.data_dir}/Val2/y_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.valid.output.y.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.valid.output.y.path}
            - ${data.dtype}
        normalization: null
  # Test set
  test:
    # Function
    _target_: data.slant_path.compute_slant_path
    _recursive_: false  # Do not instantiate nested objects
    # What the method needs as input
    input:
      # Coordinates
      lat: ${data.stage.test.vars.lat}
      lon: ${data.stage.test.vars.lon}
      pressure: ${data.stage.test.vars.pressure}
      # Meta data
      meta: ${data.stage.test.vars.meta}
      # Profiles
      prof: ${data.stage.test.vars.prof}
    output:
      lat:
        # Output path
        path: ${paths.data_dir}/Test2/lat_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.test.output.lat.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.test.output.lat.path}
            - ${data.dtype}
        normalization: ${data.stage.test.vars.lat.normalization}
      lon:
        # Output path
        path: ${paths.data_dir}/Test2/lon_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.test.output.lon.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.test.output.lon.path}
            - ${data.dtype}
        normalization: ${data.stage.test.vars.lon.normalization}
      z:
        # Output path
        path: ${paths.data_dir}/Test2/z_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.test.output.z.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.test.output.z.path}
            - ${data.dtype}
        normalization: null
      x:
        # Output path
        path: ${paths.data_dir}/Test2/x_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.test.output.x.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.test.output.x.path}
            - ${data.dtype}
        normalization: null
      y:
        # Output path
        path: ${paths.data_dir}/Test2/y_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${preparation.slant.test.output.y.path}
        # Load function
        load:
          _target_: data.io.load_npy
          _args_:
            - ${preparation.slant.test.output.y.path}
            - ${data.dtype}
        normalization: null