# Format:
#   -- all: common configuration for all stages (train/valid/test/predict)
#   -- train/valid/test: stage-specific configuration, inheriting from all and overriding as

# Stage-specific settings (e.g., file splits, wavelengths, variables). Predict stage can simply reference test stage if identical.

# Training set
train:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Clear mask
    clear_mask:
      # Dataset class
      _target_: code.loader.dataset.EagerDataset
      _recursive_: false
      # Type of data
      type: 'Clear mask'
      # Loading function (clear filter is computed, not loaded directly)
      load:
        _target_: code.preprocessing.mask.clear_mask
        _args_:
          - ${....prof.load}
      # Transformations to apply
      transformations: null
# Validation set
valid:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Clear mask
    clear_mask:
      # Dataset class
      _target_: ${....train.variables.clear_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.clear_mask.type}
      # Loading function (clear filter is computed, not loaded directly)
      load:
        _target_: ${.....train.variables.clear_mask.load._target_}
        _args_:
          - ${....prof.load}
      # Transformations to apply
      transformations: ${....train.variables.clear_mask.transformations}
# Test set
test:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Clear mask
    clear_mask:
      # Dataset class
      _target_: ${....train.variables.clear_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.clear_mask.type}
      # Loading function (clear filter is computed, not loaded directly)
      load:
        _target_: ${.....train.variables.clear_mask.load._target_}
        _args_:
          - ${....prof.load}
      # Transformations to apply
      transformations: ${....train.variables.clear_mask.transformations}
# All data (complete dataset)
all:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Clear mask
    clear_mask:
      # Dataset class
      _target_: ${....train.variables.clear_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.clear_mask.type}
      # Loading function
      load:
        _target_: numpy.concatenate
        _args_:
          - - ${.......train.variables.clear_mask.load}
            - ${.......valid.variables.clear_mask.load}
            - ${.......test.variables.clear_mask.load}
          - 0
      # Transformations to apply
      transformations: null