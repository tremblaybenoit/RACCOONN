# Format:
#   stage:
#   -- source: common configuration for all stages (train/valid/test/predict)
#   -- train/valid/test: stage-specific configuration, inheriting from source and overriding as

# Stage-specific settings (e.g., file splits, wavelengths, variables). Predict stage can simply reference test stage if identical.
stage:
  # Training set
  train:
    # Variable-specific configuration, keyed by variable name for potential multi-variable support.
    variables:
      # Z coordinate (slant)
      z_slant: &train
        # Dataset class
        _target_: forward.model.data_loader.TorchDataset
        _recursive_: false
        # Path to the z slant coordinate file
        path: ${....dir}/z_slant.npy
        # Loading function
        load:
          _target_: data.io.load_npy
          path: ${..path}
          dtype: ${data.dtype}
        # Transformations to apply
        transformations:
          # Per-variable normalization
          normalization:
            _target_: data.transformations.min_max
            _partial_: true
            stats:
              _target_: data.statistics.read_statistics_var
              path: ${preparation.statistics.data.output.path}
              var: 'z_slant'
              dtype: ${data.dtype}
  # Validation set
  valid:
    # Variable-specific configuration, keyed by variable name for potential multi-variable support.
    variables:
      # Z coordinate (slant)
      z_slant:
        <<: *train
  # Test set
  test:
    # Variable-specific configuration, keyed by variable name for potential multi-variable support.
    variables:
      # Z coordinate (slant)
      z_slant:
          <<: *train
    # Results-specific configuration for the test stage
    results:
      # Z coordinate (slant)
      z_slant:
        # Path to the z slant results file
        path: ${paths.run_dir}/test_z_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${..path}
        load:
          _target_: data.io.load_npy
          path: ${..path}
          dtype: ${data.dtype}
  # Predict set
  predict:
    # Variable-specific configuration, keyed by variable name for potential multi-variable support.
    variables:
      # Z coordinate (slant)
      z_slant:
        <<: *train
    # Results-specific configuration for the predict stage
    results:
      # Z coordinate (slant)
      z_slant:
        # Path to the z slant results file
        path: ${paths.run_dir}/predict_z_slant.npy
        # Save function
        save:
          _target_: numpy.save
          _partial_: true
          _args_:
            - ${..path}
        load:
          _target_: data.io.load_npy
          path: ${..path}
          dtype: ${data.dtype}
  # Source data (complete dataset)
  source:
    # Variable-specific configuration, keyed by variable name for potential multi-variable support.
    variables:
      # Z coordinate (slant)
      z_slant:
        # Loading function
        load:
          _target_: numpy.concatenate
          _args_:
            - - ${data.stage.train.variables.z_slant.load}
              - ${data.stage.valid.variables.z_slant.load}
              - ${data.stage.test.variables.z_slant.load}
            - 0
        # Transformations to apply
        transformations: ${data.stage.train.variables.z_slant.transformations}

