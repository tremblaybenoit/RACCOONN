defaults:
  - synthetic/cloud_filter@stage
  - synthetic/daytime_filter@stage
  - synthetic/hofx@stage
  - synthetic/lat@stage
  - synthetic/lat_slant@stage
  - synthetic/lon@stage
  - synthetic/lon_slant@stage
  - synthetic/meta@stage
  - synthetic/obs@stage
  - synthetic/pressure@stage
  - synthetic/pressure_log@stage
  - synthetic/prof@stage
  - synthetic/scans@stage
  - synthetic/surf@stage
  - synthetic/x_slant@stage
  - synthetic/y_slant@stage
  - synthetic/z_slant@stage

# dtype
dtype: 'float32'

# Split the data into training, validation, and test sets
stage:
  # Training set
  train:
    # Path to training data
    dir: ${paths.data_dir}/Train2
    # Split
    split: null

  # Validation set
  valid:
    # Path to validation data
    dir: ${paths.data_dir}/Val2
    # Split
    split: null

  # Test set
  test:
    # Path to test data
    dir: ${paths.data_dir}/Test2
    # Split
    split: null

  # Prediction set (NOTE: By default, we use the test set as the prediction set for demonstration purposes)
  predict:
    # Path to prediction data
    dir: ${paths.data_dir}/Test2
    # Split
    split: null