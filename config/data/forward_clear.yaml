defaults:
  - synthetic/default@stage
  - synthetic/default@source

# dtype
dtype: 'float32'

# Variables to use as input/output
select_input: ['prof', 'meta', 'surf']
select_target: ['bt_crtm']
select_context: ['clear_mask', 'cloud_mask', 'daytime_mask', 'nighttime_mask']
select_output: ['bt_forward', 'bt_forward_stdev']

# Original data - Split the data into training, validation, and test sets
source:
  # Training set
  train:
    # Path to training data
    dir: ../synthetic_float32/Train2
    # Split
    split: null
    # Variables
    variables:
      # Atmospheric profiles
      prof:
        # Types of profiles
        type: ['Air temperature', 'Humidity mixing ratio', 'Ozone mixing ratio']
  # Validation set
  valid:
    # Path to validation data
    dir: ../synthetic_float32/Val2
    # Split
    split: null

  # Test set
  test:
    # Path to test data
    dir: ../synthetic_float32/Test2
    # Split
    split: null

  # Prediction set
  predict: null

# Split the data into training, validation, and test sets
stage:
  # Training set
  train:
    # Path to training data
    dir: ${paths.data_dir}/Train2
    # Split
    split: null

  # Validation set
  valid:
    # Path to validation data
    dir: ${paths.data_dir}/Val2
    # Split
    split: null

  # Test set
  test:
    # Path to test data
    dir: ${paths.data_dir}/Test2
    # Split
    split: null

  # Prediction set (NOTE: By default, we use the test set as the prediction set for demonstration purposes)
  predict:
    # Path to prediction data
    dir: ${data.stage.test.dir}
    # Split
    split: ${data.stage.test.split}