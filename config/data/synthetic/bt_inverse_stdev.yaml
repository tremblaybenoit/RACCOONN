# Format:
#   -- all: common configuration for all stages (train/valid/test/predict)
#   -- train/valid/test: stage-specific configuration, inheriting from all and overriding as

# Stage-specific settings (e.g., file splits, wavelengths, variables). Predict stage can simply reference test stage if identical.

# Training set
train:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # HOFX (Simulated Observations)
    bt_inverse_stdev:
      # Dataset class
      _target_: code.loader.dataset.EagerDataset
      _recursive_: false
      # Path to the HOFX (simulated observations) file
      path: ${paths.run_dir}/bt_inverse_stdev_train.npy
      # Channels
      type: ${..bt_inverse.type}
      # Number of variables
      n: ${..bt_inverse.n}
      # Loading function
      load:
        _target_: code.data.io.load_npy
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: code.data.io.save_npy
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: null
# Validation set
valid:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # HOFX (Simulated Observations)
    bt_inverse_stdev:
      # Dataset class
      _target_: ${....train.variables.bt_inverse_stdev._target_}
      _recursive_: false
      # Path to the HOFX (simulated observations) file
      path: ${paths.run_dir}/bt_inverse_stdev_valid.npy
      # Channels
      type: ${....train.variables.bt_inverse_stdev.type}
      # Number of variables
      n: ${....train.variables.bt_inverse_stdev.n}
      # Loading function
      load:
        _target_: ${.....train.variables.bt_inverse_stdev.load._target_}
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: ${.....train.variables.bt_inverse_stdev.save._target_}
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: ${....train.variables.bt_inverse_stdev.transformations}
# Test set
test:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # HOFX (Simulated Observations)
    bt_inverse_stdev:
      # Dataset class
      _target_: ${....train.variables.bt_inverse_stdev._target_}
      _recursive_: false
      # Path to the HOFX (simulated observations) file
      path: ${paths.run_dir}/bt_inverse_stdev_test.npy
      # Channels
      type: ${....train.variables.bt_inverse_stdev.type}
      # Number of variables
      n: ${....train.variables.bt_inverse_stdev.n}
      # Loading function
      load:
        _target_: ${.....train.variables.bt_inverse_stdev.load._target_}
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: ${.....train.variables.bt_inverse_stdev.save._target_}
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: ${....train.variables.bt_inverse_stdev.transformations}
# Predict set
predict:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # HOFX (Simulated Observations)
    bt_inverse_stdev:
      # Dataset class
      _target_: ${....train.variables.bt_inverse_stdev._target_}
      _recursive_: false
      # Path to the HOFX (simulated observations) file
      path: ${paths.run_dir}/bt_inverse_stdev_predict.npy
      # Channels
      type: ${....train.variables.bt_inverse_stdev.type}
      # Number of variables
      n: ${....train.variables.bt_inverse_stdev.n}
      # Loading function
      load:
        _target_: ${.....train.variables.bt_inverse_stdev.load._target_}
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: ${.....train.variables.bt_inverse_stdev.save._target_}
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: ${....train.variables.bt_inverse_stdev.transformations}
# All data (complete dataset)
all:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # HOFX (Simulated Observations)
    bt_inverse_stdev:
      # Dataset class
      _target_: ${....train.variables.bt_inverse_stdev._target_}
      _recursive_: false
      # Channels
      type: ${....train.variables.bt_inverse_stdev.type}
      # Number of variables
      n: ${....train.variables.bt_inverse_stdev.n}
      # Loading function
      load:
        _target_: numpy.concatenate
        _args_:
          - - ${.......train.variables.bt_inverse_stdev.load}
            - ${.......valid.variables.bt_inverse_stdev.load}
            - ${.......test.variables.bt_inverse_stdev.load}
          - 0
      # Transformations to apply
      transformations: ${....train.variables.bt_inverse_stdev.transformations}