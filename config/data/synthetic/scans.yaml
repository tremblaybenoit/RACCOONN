# Format:
#   -- all: common configuration for all stages (train/valid/test/predict)
#   -- train/valid/test: stage-specific configuration, inheriting from all and overriding as

# Stage-specific settings (e.g., file splits, wavelengths, variables). Predict stage can simply reference test stage if identical.

# Training set
train:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Scans
    scans:
      # Dataset class
      _target_: code.loader.dataset.EagerDataset
      _recursive_: false
      # Path to the scans data file
      path: ${...dir}/scans.npy
      # Type of scans
      type: 'Time'
      # Loading function
      load:
        _target_: code.data.io.load_npy
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: code.data.io.save_npy
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations:
        # Per-variable normalization
        normalization:
          _target_: code.data.transformations.min_max
          _partial_: true
          stats:
            _target_: code.preprocessing.statistics.read_statistics_var
            load: ${preprocessing.statistics.output.load}
            key: 'scans'
            dtype: ${data.dtype}
# Validation set
valid:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Scans
    scans:
      # Dataset class
      _target_: ${....train.variables.scans._target_}
      _recursive_: false
      # Path to the scans data file
      path: ${...dir}/scans.npy
      # Type of scans
      type: ${....train.variables.scans.type}
      # Loading function
      load:
        _target_: ${.....train.variables.scans.load._target_}
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: ${.....train.variables.scans.save._target_}
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: ${....train.variables.scans.transformations}
# Test set
test:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Scans
    scans:
      # Dataset class
      _target_: ${....train.variables.scans._target_}
      _recursive_: false
      # Path to the scans data file
      path: ${...dir}/scans.npy
      # Type of scans
      type: ${....train.variables.scans.type}
      # Loading function
      load:
        _target_: ${.....train.variables.scans.load._target_}
        path: ${..path}
        split: ${....split}
        dtype: ${data.dtype}
      # Save function
      save:
        _target_: ${.....train.variables.scans.save._target_}
        _partial_: true
        _args_:
          - ${...path}
        dtype: ${data.dtype}
      # Transformations to apply
      transformations: ${....train.variables.scans.transformations}
# Predict set
predict:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Scans
    scans: ${...test.variables.scans}
# All data (complete dataset)
all:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Scans
    scans:
      # Dataset class
      _target_: ${....train.variables.scans._target_}
      _recursive_: false
      # Type of scans
      type: ${....train.variables.scans.type}
      # Loading function
      load:
        _target_: numpy.concatenate
        _args_:
          - - ${.......train.variables.scans.load}
            - ${.......valid.variables.scans.load}
            - ${.......test.variables.scans.load}
          - 0
      # Transformations to apply
      transformations: ${....train.variables.scans.transformations}