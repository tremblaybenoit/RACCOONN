# Format:
#   -- all: common configuration for all stages (train/valid/test/predict)
#   -- train/valid/test: stage-specific configuration, inheriting from all and overriding as

# Stage-specific settings (e.g., file splits, wavelengths, variables). Predict stage can simply reference test stage if identical.

# Training set
train:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Daytime mask
    daytime_mask:
      # Dataset class
      _target_: code.loader.dataset.EagerDataset
      _recursive_: false
      # Type of data
      type: 'Daytime mask'
      # Loading function (daytime filter is computed, not loaded directly)
      load:
        _target_: code.preprocessing.mask.daytime_mask
        _args_:
          - ${....meta.load}
      # Transformations to apply
      transformations: null
# Validation set
valid:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Daytime mask
    daytime_mask:
      # Dataset class
      _target_: ${....train.variables.daytime_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.daytime_mask.type}
      # Loading function (daytime filter is computed, not loaded directly)
      load:
        _target_: ${.....train.variables.daytime_mask.load._target_}
        _args_:
          - ${....meta.load}
      # Transformations to apply
      transformations: ${....train.variables.daytime_mask.transformations}
# Test set
test:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Daytime mask
    daytime_mask:
      # Dataset class
      _target_: ${....train.variables.daytime_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.daytime_mask.type}
      # Loading function (daytime filter is computed, not loaded directly)
      load:
        _target_: ${.....train.variables.daytime_mask.load._target_}
        _args_:
          - ${....meta.load}
      # Transformations to apply
      transformations: ${....train.variables.daytime_mask.transformations}
# Predict set
predict:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Daytime mask
    daytime_mask: ${...test.variables.daytime_mask}
# All data (complete dataset)
all:
  # Variable-specific configuration, keyed by variable name for potential multi-variable support.
  variables:
    # Daytime mask
    daytime_mask:
      # Dataset class
      _target_: ${....train.variables.daytime_mask._target_}
      _recursive_: false
      # Type of data
      type: ${....train.variables.daytime_mask.type}
      # Loading function
      load:
        _target_: numpy.concatenate
        _args_:
          - - ${.......train.variables.daytime_mask.load}
            - ${.......valid.variables.daytime_mask.load}
            - ${.......test.variables.daytime_mask.load}
          - 0
      # Transformations to apply
      transformations: null