# @package _global_
# Experiment-specific Hydra structure

# Overriding default structure
defaults:
  - override /paths: default
  - override /hydra: default
  - override /data: forward_default
  - override /preparation: forward_default
  - override /loader: default
  - override /model: forward_default
  - override /trainer: gpu
  - override /callbacks: forward_default
  - override /logger: default

# All parameters below will be merged with parameters from default configurations set above.
# This allows you to overwrite only specified parameters

# Task name. Note that it determines the output directory path
task_name: "forward"  # Experiment

# Run id (unique identifier for the run)
paths:
  task_dir: "../forward_outputs"
  run_id: "default"
  data_dir: ../data_${data.dtype}
  checkpoint_dir: "forward/model/checkpoints"

# Data configuration
data:
  # Precision
  dtype: 'float32'
  # Split ratios for training, validation, and test sets
  stage:
    # Training set
    train:
      # Variables
      variables:
        # Atmospheric profiles
        prof:
          # Transformations
          transformations:
            # Normalization function and parameters
            normalization:
              # Function
              _target_: data.transformations.NormalizeProfiles
              # Min/max values for normalization
              profmin:
                _target_: numpy.loadtxt
                _args_:
                  - data/normalization/prof_m.txt
              profmax:
                _target_: numpy.loadtxt
                _args_:
                  - data/normalization/prof_div.txt
              dtype: ${data.dtype}
    # Validation set
    valid:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Transformations
          transformations: ${data.stage.train.vars.prof.transformations}
    # Test set
    test:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Transformations
          transformations: ${data.stage.train.vars.prof.transformations}
    # Prediction set
    predict:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Transformations
          transformations: ${data.stage.train.vars.prof.transformations}

# Logger configuration
logger:
  # MLflow logger
  ports:
    mlflow: 5001