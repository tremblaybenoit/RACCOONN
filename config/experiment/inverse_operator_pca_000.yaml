# @package _global_
# Experiment-specific Hydra structure

# Overriding default structure
defaults:
  - override /paths: default
  - override /hydra: default
  - override /data: inverse_recast
  - override /preparation: inverse_default
  - override /loader: inverse_cs
  - override /model: inverse_operator
  - override /trainer: cpu
  - override /callbacks: inverse_default
  - override /logger: default

# All parameters below will be merged with parameters from default configurations set above.
# This allows you to overwrite only specified parameters

# Task name. Note that it determines the output directory path
task_name: "inverse_operator_recast"  # Experiment

# Run id (unique identifier for the run)
paths:
  task_dir: "../inverse_outputs"
  run_id: "pca_000"
  data_dir: ../data_${data.dtype}_cs

# Trainer configuration
trainer:
  min_epochs: 1  # To prevent early stopping
  max_epochs: 64  # Number of epochs
  precision: 32  # 32-bit precision

# Initialization
# init_from_checkpoint: inverse/model/checkpoints/model_bg_200.ckpt

# Callbacks
callbacks:
  early_stopping:
    patience: 10  # Number of epochs with no improvement after which training will be stopped

# Preparation configuration
preparation:
  # Statistics computation
  statistics:
    # Variables for which to compute statistics
    data:
      # Output path for statistics
      output:
        path: ${paths.data_dir}/normalization/statistics.pkl
  # Covariance computation
  covariance:
    # Error covariance matrix in model space
    B:
      # Output path for covariance matrix B
      output:
        path: ${paths.data_dir}/normalization/covariance_matrix_B_inverse.npy
    # Error covariance matrix in observation space
    R:
      # Output path for covariance matrix R
      output:
        path: ${paths.data_dir}/normalization/covariance_matrix_R_inverse.npy
  # PCA decomposition
  pca:
    # Atmospheric profiles
    prof:
      # Function
      _target_: data.pca.compute_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        # Profile data to compute PCA on
        data:
          load:
            _target_: data.io.load_stack_and_normalize
            _recursive_: false
            stack:
              - ${data.stage.train.vars.prof}
              - ${data.stage.valid.vars.prof}
              - ${data.stage.test.vars.prof}
        n_comp: 270
      # What the method produces as output (pickle file)
      output:
        # File path to save/load the PCA object
        path: ${paths.data_dir}/normalization/pca.pkl
        # Save function
        save:
          _target_: data.io.save_pkl
          _partial_: true
          _args_:
            - ${preparation.pca.prof.output.path}
        # Load function
        load:
          _target_: data.io.load_pkl
          _args_:
            - ${preparation.pca.prof.output.path}
    prof_train:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
        # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
              - ${data.stage.train.vars.prof}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/pca_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_train.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_train.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/white_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_train.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_train.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/sym_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_train.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_train.output.pca_sym.path}
              - ${data.dtype}
          normalization: null
    prof_valid:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
        # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
              - ${data.stage.valid.vars.prof}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/pca_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_valid.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_valid.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/white_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_valid.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_valid.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/sym_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_valid.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_valid.output.pca_sym.path}
              - ${data.dtype}
          normalization: null
    prof_test:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
          # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
            - ${data.stage.test.vars.prof}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/pca_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_test.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_test.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/white_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_test.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_test.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/sym_prof.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_test.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_test.output.pca_sym.path}
              - ${data.dtype}
          normalization: null
    prof_background_train:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
        # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
              - ${data.stage.train.vars.prof_background}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/pca_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_train.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_train.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/white_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_train.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_train.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Train2/sym_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_train.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_train.output.pca_sym.path}
              - ${data.dtype}
          normalization: null
    prof_background_valid:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
        # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
              - ${data.stage.valid.vars.prof_background}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/pca_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/white_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Val2/sym_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_valid.output.pca_sym.path}
              - ${data.dtype}
          normalization: null
    prof_background_test:
      # Function
      _target_: data.pca.project_pca
      _recursive_: false  # Do not instantiate nested objects
      # What the method needs as input
      input:
        pca_buffers: ${preparation.pca.prof.output}
          # Profile data to project
        data:
          load:
            _target_: data.io.load_var_and_normalize
            _recursive_: false
            _args_:
              - ${data.stage.test.vars.prof_background}
      output:
        pca:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/pca_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_test.output.pca.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_test.output.pca.path}
              - ${data.dtype}
          normalization: null
        pca_white:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/white_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_test.output.pca_white.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_test.output.pca_white.path}
              - ${data.dtype}
          normalization: null
        pca_sym:
          # Output path for PCA projection
          path: ${paths.data_dir}/Test2/sym_prof_background.npy
          # Save function
          save:
            _target_: numpy.save
            _partial_: true
            _args_:
              - ${preparation.pca.prof_background_test.output.pca_sym.path}
          # Load function
          load:
            _target_: data.io.load_npy
            _args_:
              - ${preparation.pca.prof_background_test.output.pca_sym.path}
              - ${data.dtype}
          normalization: null


# Data configuration
data:
  # Precision
  dtype: 'float32'
  # Split ratios for training, validation, and test sets
  stage:
    # Training set
    train:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Normalization function and parameters
          normalization:
            _target_: data.transformations.min_max
            _partial_: true
            stats:
              _target_: data.statistics.read_statistics_var
              path: ${preparation.statistics.data.output.path}
              var: 'prof'
              dtype: ${data.dtype}
            axis: 1
        #
        pca_coeffs:
          # Loading function
          load:
            _target_: data.pca.load_pca_buffers
            path: ${preparation.pca.prof.output.path}
            buffer: 'scales'
          normalization: null

    # Validation set
    valid:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Normalization function and parameters
          normalization: ${data.stage.train.vars.prof.normalization}
    # Test set
    test:
      # Variables
      vars:
        # Atmospheric profiles
        prof:
          # Normalization function and parameters
          normalization: ${data.stage.train.vars.prof.normalization}

# Loader configuration
loader:
  batch_size: 96
  num_workers: 10
  persistent_workers: true
  pin_memory: true
# Model parameters
model:
  # Model
  _target_: inverse.model.model.PINNverseOperator5
  # Optimizer function and parameters
  optimizer:
    # Learning rate
    lr: 0.0005
    # Weight decay (L2 regularization)
    weight_decay: 0
  # Learning rate scheduler
  lr_scheduler:
    # Function
    _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
    # Mode for the scheduler
    mode: min
    # Factor to reduce the learning rate
    factor: 0.2
    # Number of epochs with no improvement after which learning rate will be reduced
    patience: 3
    # Minimum learning rate
    min_lr: 1.0e-6
    # Threshold for measuring the new optimum, to only focus on significant changes
    threshold: 0.0
    threshold_mode: abs
  # Model parameters associated with the data (e.g., input/output dimensions) and architecture
  parameters:
    # Model architecture
    architecture:
      # Number of neurons in the hidden layers
      n_neurons: 128
      # Number of hidden layers
      n_layers: 2
      # Dropout rate
      dropout: 0.1
    # Data
    data:
      n_prof: 3  # Number of profile variables
  # Loss function and parameters
  loss_func:
    # Weights for observation (obs), model, and boundary condition (bcs) errors
    lambda_obs: 0
    lambda_model: 1.
    lambda_bcs: 1.
    clear_sky: true
    # Loss term for observations
    loss_obs:
      # _target_: inverse.model.loss.MSE
      _target_: inverse.model.loss.DiagonalQuadraticForm
    # Loss term for model variables
    loss_model:
      _target_: inverse.model.loss.MSE
      # _target_: inverse.model.loss.QuadraticForm
      # matrix: ${preparation.covariance.B.output.load}
    # Loss term for boundary conditions
    loss_bcs:
      _target_: inverse.model.loss.MSE
    # Pressure filter: Consider only pressure levels with non-zero variance (for model and bcs errors)
    pressure_filter: ${data.pressure_filter.load}
    # Prediction profile: Use full profile for loss computation
    prof_pred:
      # Normalization function and parameters
      _target_: numpy.zeros
      shape: [96, 9, 127]
      dtype: ${data.dtype}
  # Positional encoding
  positional_encoding:
    _target_: inverse.model.encoding.MultiScaleGaussianEncoding
    num_freqs: 20
    d_input: 4
    # [Lat, Lon, Scans, Pressure]
    sigma_per_dim: [ 1.0, 1.0, 0.5]
  # Activation functions (input layer/within the network)
  activation_in:
    _target_: forward.model.activation.Sine
  # Activation function (output layer)
  activation_out:
    _target_: torch.nn.Identity()