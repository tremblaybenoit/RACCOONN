# Note: The order of defaults determines the order in which configs override each other.
defaults:
  - paths: default
  - hydra: default
  - data: inverse_default
  - preprocessing: inverse_default
  - loader: default
  - model: inverse_default
  - trainer: cpu
  - callbacks: inverse_default
  - logger: default
  - _self_
